/*
 * Render blog templates
 *
 * Adrian Lange 2/2014
 */

function renderBlogPage(blogTmpl, pathname) {
  var renderSuccess = true;
  var page = blogTmpl;

  return [renderSuccess, page];
}

exports.renderBlogPage = renderBlogPage;
